/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn.weatherapi.com'], // Ajoutez le domaine externe ici
    },
  };
  
  // Utilisez `export default` pour un module ES
  export default nextConfig;
  